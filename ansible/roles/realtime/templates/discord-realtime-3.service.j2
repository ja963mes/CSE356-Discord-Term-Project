[Unit]
Description=Discord realtime service (instance 3)
After=network.target

[Service]
Type=simple
User={{ deploy_user }}
WorkingDirectory={{ deploy_path }}
EnvironmentFile=/etc/discord.env
Environment="REALTIME_PORT={{ realtime_port_3 | default(3013) }}"
Environment="REALTIME_INTERNAL_URL=http://{{ realtime_internal_host | default(ansible_default_ipv4.address) }}:{{ realtime_port_3 | default(3013) }}"
Environment="LOG_PRETTY=false"
ExecStart=/usr/bin/node {{ deploy_path }}/services/realtime/dist/cluster.js
Restart=always
RestartSec=3
StandardOutput=journal
StandardError=journal
LimitNOFILE=infinity

[Install]
WantedBy=multi-user.target
