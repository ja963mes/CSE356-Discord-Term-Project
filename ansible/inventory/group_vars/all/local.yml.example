# Local-only Ansible overrides. Copy to `local.yml` and keep the real values
# out of git.

# manage_zabbix_agent: true
# zabbix_agent_server: "{{ zabbix_agent_server }}"
# zabbix_agent_server_active: "{{ zabbix_agent_server }}"
